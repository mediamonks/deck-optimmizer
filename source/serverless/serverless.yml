app: deckoptimmizer
org: mirkovanw
service: deckoptimmizer

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs16.x
  stage: dev
  region: eu-central-1

functions:
  optimize:
    handler: handler.optimize
    layers:
      - { Ref: GifsicleLambdaLayer }
    url: true
    environment:
      BUCKET: ${param:BUCKET}
      KEY_ID: ${param:ACCESSKEYID}
      KEY_SECRET: ${param:SECRETACCESSKEY}

layers:
  gifsicle:
    path: layer